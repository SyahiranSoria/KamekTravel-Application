import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'KamekTravel Application',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
